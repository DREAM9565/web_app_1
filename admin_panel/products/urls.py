# products/urls.py
from django.urls import path
from . import views

urlpatterns = [
    # Пример маршрутов для приложения products:
    # path('', views.product_list, name='product_list'),
    # path('<str:encode>/', views.product_detail, name='product_detail'),
]
